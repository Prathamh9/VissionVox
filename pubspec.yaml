name: vision_vox
description: Assistive app for visually impaired (Vision Vox)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_tts: ^3.8.3
  shared_preferences: ^2.2.3
  web_socket_channel: ^2.4.5
  connectivity_plus: ^6.0.5
  permission_handler: ^11.3.1
  camera: ^0.10.6
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.13.0
  # For future on-device OCR/Object Detection (uncomment when you’re ready)
  # google_mlkit_text_recognition: ^0.12.0
  # google_mlkit_object_detection: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
